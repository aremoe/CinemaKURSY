﻿@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Доступні фільми</h1>
</div>
    <div class="row row-cols-1 row-cols-md-3 row-cols-lg-4 g-4">
        @foreach (var i in Model)
        {
            <div class="col">
                <partial name="_CardPartial" model="@i" />
            </div>
        }
    </div>
